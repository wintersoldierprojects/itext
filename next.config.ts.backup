import type { NextConfig } from "next";

// Import Serwist with CommonJS require for better compatibility
const withSerwist = require("@serwist/next").default;

// Next.js configuration
const nextConfig: NextConfig = {
  images: {
    remotePatterns: [
      {
        protocol: 'https',
        hostname: 'api.dicebear.com',
        port: '',
        pathname: '/**',
      },
      {
        protocol: 'https',
        hostname: 'lh3.googleusercontent.com',
        port: '',
        pathname: '/**',
      }
    ],
    dangerouslyAllowSVG: true,
    contentSecurityPolicy: "default-src 'self'; script-src 'none'; sandbox;",
  },
  // Next.js configuration options  
  typescript: {
    ignoreBuildErrors: true,
  },
  eslint: {
    ignoreDuringBuilds: true,
  },
  // Turbopack configuration (Next.js 15+ recommended)
  experimental: {
    optimizePackageImports: ['@supabase/supabase-js'],
  },
  // Turbopack configuration (stable in Next.js 15+)
  turbopack: {
    resolveAlias: {
      canvas: './empty-module.js',
    },
  },
  // Additional optimizations for Turbopack
  poweredByHeader: false,
  compress: true,
  // Optimize bundle size
  output: 'standalone',
  // Dev indicators configuration
  devIndicators: {
    position: 'bottom-right',
  },
};

// Export with Serwist configuration
export default withSerwist(nextConfig, {
  swSrc: "app/sw.ts",
  swDest: "public/sw.js",
  cacheOnFrontEndNav: true,
  reloadOnOnline: true,
  disable: process.env.NODE_ENV === "development",
});
